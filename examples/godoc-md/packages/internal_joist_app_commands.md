# Package: internal/joist/app/commands

Module: `github.com/JLugagne/joist/internal/joist/app/commands`

## Exported Symbols

### internal/joist/app/commands/scaffolder.go

```go
No matches found for 'internal/joist/app/commands/scaffolder.go'.
Hint: run 'go-surgeon graph -s -d .' to list available symbols, or check the Receiver.Method format.
```

### type ScaffolderHandler struct { fs filesystem.FileSystem }

```go
No matches found for 'type ScaffolderHandler struct { fs filesystem.FileSystem }'.
Hint: run 'go-surgeon graph -s -d .' to list available symbols, or check the Receiver.Method format.
```

### func NewScaffolderHandler(fs filesystem.FileSystem) *ScaffolderHandler

```go
No matches found for 'func NewScaffolderHandler(fs filesystem.FileSystem) *ScaffolderHandler'.
Hint: run 'go-surgeon graph -s -d .' to list available symbols, or check the Receiver.Method format.
```

### func (h *ScaffolderHandler) ListTemplates(ctx context.Context) ([]domain.Template, error)

```go
No matches found for 'func (h *ScaffolderHandler) ListTemplates(ctx context.Context) ([]domain.Template, error)'.
Hint: run 'go-surgeon graph -s -d .' to list available symbols, or check the Receiver.Method format.
```

### func (h *ScaffolderHandler) GetTemplate(ctx context.Context, templateName string) (domain.Template, error)

```go
No matches found for 'func (h *ScaffolderHandler) GetTemplate(ctx context.Context, templateName string) (domain.Template, error)'.
Hint: run 'go-surgeon graph -s -d .' to list available symbols, or check the Receiver.Method format.
```

### func (h *ScaffolderHandler) Execute(ctx context.Context, templateName, commandName string, params map[string]string, runCommands bool) error

```go
No matches found for 'func (h *ScaffolderHandler) Execute(ctx context.Context, templateName, commandName string, params map[string]string, runCommands bool) error'.
Hint: run 'go-surgeon graph -s -d .' to list available symbols, or check the Receiver.Method format.
```

### func (h *ScaffolderHandler) Lint(ctx context.Context, templateName string) []domain.LintError

```go
No matches found for 'func (h *ScaffolderHandler) Lint(ctx context.Context, templateName string) []domain.LintError'.
Hint: run 'go-surgeon graph -s -d .' to list available symbols, or check the Receiver.Method format.
```

